import React from 'react';

const Login = ()=> {
    return (
        <form>
            <input />
            <input />
            <button>Login</button>
        </form>
    );
};

export default Login;